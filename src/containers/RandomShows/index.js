import React, { Component } from 'react';
import axios from 'axios';

export default class RandomShows extends Component {
    fetchShows() {
        axios.get('')
    }

    render() {
        return (
            <div>

            </div>
        );
    }
}